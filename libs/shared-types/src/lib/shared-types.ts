/**
 * Placeholder types for VoxPopuli shared-types library.
 * Full type definitions will be added by Agent 2 (AI-102).
 */

/** Health endpoint response */
export interface HealthResponse {
  status: string;
  uptime: number;
}
